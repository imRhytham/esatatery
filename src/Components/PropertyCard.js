import React from 'react';

const PropertyCard = ({
	name,
	image,
	price,
	address,
	beds,
	bathrooms,
	area,
}) => {
	return (
		<div className='property-card'>
			<div></div>
		</div>
	);
};

export default PropertyCard;
